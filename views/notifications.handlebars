<!DOCTYPE html>
<html lang="en" class="no-js">
    <head>
        <meta charset="UTF-8" />
        <title>Notifications</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
        <link rel="shortcut icon" href="/images/favicon.jpg">
        <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
        <link rel="stylesheet" type="text/css" href="/css/bootstrap/bootstrap.css">
        <link rel="stylesheet" type="text/css" href="/css/bootstrap/bootstrap-theme.css">
        <link rel="stylesheet" type="text/css" href="/css/navbar.css">
        <link rel="stylesheet" type="text/css" href="/css/background.css">
        <link rel="stylesheet" type="text/css" href="/css/notifications.css">
        {{> woopra_tracking_script}}
        {{> woopra_tracking_user_script}}
    </head>
    <body>
        {{> navbar}}

        {{#each notifications}}
            <!-- Create a profile modal for each user-->
            {{> profile_modal}}
        {{/each}}

        <div class="container">
            <div class="row">
                <div class="col-sm-10 col-sm-offset-1">
                    <!-- Header -->
                    <div class="page-header">
                        <h1 id="homepage-header">Notifications</h1>
                    </div>    
                    <div class="tabs">
                        <div class="tab-content">

                            <!-- Show this if there are no notifications -->
                            {{#if empty}}
                                <p><h2>You currently have no notifications!</h2></p> <p><h3>Post a project idea in the <a href="/homepage">homepage</a> and people may notify you if they are interested here.</h3></p>
                            {{/if}}

                            <!-- Message Tab -->
                            <div id="all-projects" class="tab-pane active">
                                <ul class="media-list">

                                    {{#each notifications}}

                                    <!-- Notifications -->
                                    <li class="media">
                                       
                                        <div class="media-body">
                                         
                                            <div class="well well-lg notification-post {{post-border}}">
                                                <h6 class="post-time">{{createdAt}}</h6>

                                                <h4 class="project-title">{{project}}</h4>

                                                <p class="project-from">
                                                    <label>User:</label> <a href="#profileModal{{fromID}}" data-toggle="modal">{{from}}</a>
                                                </p>

                                                <p class="from-email">
                                                    <label>Email:</label> {{fromEmail}}
                                                </p>

                                                <p class="project-roles">
                                                    <label>Role:</label> {{roles}}
                                                </p>

                                                <div class="project-description">
                                                    <label>Message:</label> {{message}}
                                                </div>

                                                <div id="buttons-block">
                                                    <form>
                                                        <button type="submit" formaction="notifications/{{_id}}/delete" formmethod="post" class="btn btn-info btn-circle text-uppercase btn-danger"><span class="glyphicon glyphicon-remove"></span> Delete</button>
                                                    </form>
                                                <div>

                                            </div>
                                        </div>
                                    </li>
                                    {{/each}}
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </body>
    <script src="/js/jquery-2.1.3.js"></script>
    <script src="/js/bootstrap.js"></script>
    <script src="/js/notifications.js"></script>
</html>